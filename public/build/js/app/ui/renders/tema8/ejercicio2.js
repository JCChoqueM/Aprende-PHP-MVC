import{mostrarResultado}from"../../renderResult.js";export function render(t){const{accion:n,clientes:o,paginaActual:e,totalPaginas:i,mensaje:a,exito:r,cliente:c}=t.respuesta;if("listar"!==n&&n)if("confirmarBaja"===n)mostrarResultado(`\n            <p>¿Seguro que quieres eliminar a <b>${c.nombre}</b> (DNI: ${c.dni})?</p>\n            <button onclick="sendPrompt('accion=baja&id=${c.id}')">✅ Confirmar</button>\n            <button onclick="sendPrompt('accion=listar')">❌ Cancelar</button>\n        `);else{mostrarResultado(`\n            <p style="color:${!1===r?"#e74c3c":"#2ecc71"}">${a}</p>\n            <button onclick="sendPrompt('accion=listar')">← Volver al listado</button>\n        `)}else{const t=o.map(t=>`\n            <tr>\n                <td>${t.id}</td><td>${t.nombre}</td><td>${t.dni}</td><td>${t.direccion}</td>\n                <td>\n                    <button onclick="sendPrompt('accion=modificar&id=${t.id}')">✏️</button>\n                    <button onclick="sendPrompt('accion=confirmarBaja&id=${t.id}')">🗑</button>\n                </td>\n            </tr>\n        `).join(""),n=Array.from({length:i},(t,n)=>`<button onclick="sendPrompt('accion=listar&pagina=${n+1}')"\n                style="font-weight:${e===n+1?"bold":"normal"}">${n+1}</button>`).join(" ");mostrarResultado(`\n            <button onclick="sendPrompt('accion=alta')">+ Nuevo cliente</button>\n            <h3>Clientes (página ${e}/${i})</h3>\n            <table border="1">\n                <thead><tr><th>ID</th><th>Nombre</th><th>DNI</th><th>Dirección</th><th>Acciones</th></tr></thead>\n                <tbody>${t}</tbody>\n            </table>\n            <br>Páginas: ${n}\n        `)}}//# sourceMappingURL=ejercicio2.js.map
