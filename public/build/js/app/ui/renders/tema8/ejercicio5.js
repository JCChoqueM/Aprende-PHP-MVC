import{mostrarResultado}from"../../renderResult.js";export function render(t){const{accion:o,articulos:n,mensaje:c,exito:d,stockActual:a,lineas:e,baseImponible:r,iva:i,total:l,errores:s}=t.respuesta;if("listar"!==o&&o)if("venta"===o){const t=s&&s.length>0?`<ul style="color:#e74c3c">${s.map(t=>`<li>${t}</li>`).join("")}</ul>`:"",o=e.map(t=>`\n            <tr>\n                <td>${t.codigo}</td><td>${t.descripcion}</td>\n                <td>${t.cantidad}</td><td>${t.precioVenta} €</td><td>${t.subtotal} €</td>\n            </tr>`).join("");mostrarResultado(`\n            ${t}\n            <h3>🧾 Factura</h3>\n            <table border="1">\n                <thead><tr><th>Código</th><th>Descripción</th><th>Cant.</th><th>P.Venta</th><th>Subtotal</th></tr></thead>\n                <tbody>${o}</tbody>\n                <tfoot>\n                    <tr><td colspan="4">Base imponible</td><td>${r} €</td></tr>\n                    <tr><td colspan="4">IVA (21%)</td><td>${i} €</td></tr>\n                    <tr><td colspan="4"><b>Total</b></td><td><b>${l} €</b></td></tr>\n                </tfoot>\n            </table>\n            <br><button onclick="sendPrompt('accion=listar')">← Volver al listado</button>\n        `)}else{mostrarResultado(`\n            <p style="color:${!1===d?"#e74c3c":"#2ecc71"}">${c}</p>\n            ${void 0!==a?`<p>Stock actual: <b>${a}</b></p>`:""}\n            <button onclick="sendPrompt('accion=listar')">← Volver al listado</button>\n        `)}else{const t=n.map(t=>`\n            <tr>\n                <td>${t.codigo}</td><td>${t.descripcion}</td>\n                <td>${t.precioCompra} €</td><td>${t.precioVenta} €</td>\n                <td>${t.stock}</td>\n                <td>\n                    <button onclick="sendPrompt('accion=modificar&codigo=${t.codigo}')">✏️</button>\n                    <button onclick="sendPrompt('accion=baja&codigo=${t.codigo}')">🗑</button>\n                    <button onclick="sendPrompt('accion=entrada&codigo=${t.codigo}')">📥</button>\n                </td>\n            </tr>`).join("");mostrarResultado(`\n            <button onclick="sendPrompt('accion=alta')">+ Nuevo artículo</button>\n            <button onclick="sendPrompt('accion=venta')">🧾 Nueva venta</button>\n            <h3>GESTISIMAL - Almacén</h3>\n            <table border="1">\n                <thead><tr><th>Código</th><th>Descripción</th><th>P.Compra</th><th>P.Venta</th><th>Stock</th><th>Acciones</th></tr></thead>\n                <tbody>${t}</tbody>\n            </table>\n        `)}}//# sourceMappingURL=ejercicio5.js.map
