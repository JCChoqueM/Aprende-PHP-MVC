import{mostrarResultado}from"../../renderResult.js";export function render(t){const{vista:o,productos:n,detalle:r,carrito:d,total:c,mensaje:i}=t.respuesta,e=0===d.length?"<p>El carrito está vacío.</p>":`<table border="1">\n            <thead><tr><th>Producto</th><th>Precio</th><th>Uds.</th><th>Subtotal</th><th></th></tr></thead>\n            <tbody>\n                ${d.map(t=>`\n                    <tr>\n                        <td>${t.nombre}</td>\n                        <td>${t.precio} €</td>\n                        <td>${t.cantidad}</td>\n                        <td>${t.subtotal} €</td>\n                        <td><button onclick="sendPrompt('accion=eliminar&productoId=${t.id}')">🗑</button></td>\n                    </tr>\n                `).join("")}\n            </tbody>\n            <tfoot><tr><td colspan="3"><b>Total</b></td><td colspan="2"><b>${c} €</b></td></tr></tfoot>\n        </table>`;if("gestion"===o){const t=n.map(t=>`\n            <tr>\n                <td>${t.nombre}</td>\n                <td>${t.precio} €</td>\n                <td>\n                    <button onclick="sendPrompt('accion=modificarProducto&productoId=${t.id}')">✏️ Editar</button>\n                    <button onclick="sendPrompt('accion=bajaProducto&productoId=${t.id}')">🗑 Eliminar</button>\n                </td>\n            </tr>\n        `).join("");mostrarResultado(`\n            <button onclick="sendPrompt('accion=listar')">← Volver al catálogo</button>\n            <h3>Gestión de productos</h3>\n            ${i?`<p style="color:#2ecc71;">${i}</p>`:""}\n            <table border="1">\n                <thead><tr><th>Nombre</th><th>Precio</th><th>Acciones</th></tr></thead>\n                <tbody>${t}</tbody>\n            </table>\n            <br>\n            <button onclick="sendPrompt('accion=altaProducto')">+ Añadir producto</button>\n        `)}else if("detalle"===o)mostrarResultado(`\n            <button onclick="sendPrompt('accion=listar')">← Volver</button>\n            <h3>${r.nombre}</h3>\n            <img src="${r.imagen}" alt="${r.nombre}" style="max-width:200px;"><br>\n            <p>${r.descripcion}</p>\n            <b>Precio: ${r.precio} €</b><br>\n            <button onclick="sendPrompt('accion=añadir&productoId=${r.id}')">🛒 Añadir al carrito</button>\n            <hr>\n            <h3>Carrito</h3>${e}\n        `);else{const t=n.map(t=>`\n            <div style="border:1px solid #ccc; padding:10px; margin:5px; display:inline-block; width:150px;">\n                <img src="${t.imagen}" alt="${t.nombre}" style="width:100%;"><br>\n                <b>${t.nombre}</b><br>${t.precio} €<br>\n                <button onclick="sendPrompt('accion=detalle&productoId=${t.id}')">🔍 Detalle</button>\n                <button onclick="sendPrompt('accion=añadir&productoId=${t.id}')">🛒 Comprar</button>\n            </div>\n        `).join("");mostrarResultado(`\n            <button onclick="sendPrompt('accion=gestion')">⚙️ Gestionar productos</button>\n            <h3>Catálogo</h3>${t}\n            <hr>\n            <h3>Carrito</h3>${e}\n        `)}}//# sourceMappingURL=ejercicio9.js.map
