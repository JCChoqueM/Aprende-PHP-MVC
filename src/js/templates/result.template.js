// Resultado simple: número, texto, conversión, cálculo
export function renderResult(label, value, suffix = '') {
    return `
        <p>
            <strong>${label}:</strong> ${value}${suffix}
        </p>
    `;
}
// Ejercicios cubiertos

// Media

// Salario

// Área

// Volumen

// Conversores

// Potencia

// Ecuaciones